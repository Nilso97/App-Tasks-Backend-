### Terminal commands:
$ git init
$ yarn init -y
$ yarn add -D express
$ yarn add -D body-parser
$ yarn add -D nodemon
$ yarn add -D dotenv
$ yarn add -D pg-promise
$ yarn add -D axios
$ yarn add -D jest